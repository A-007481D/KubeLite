package com.ork8stra.user;

/**
 * Platform-level roles for users.
 */
public enum PlatformRole {
    // can manage all organizations and users
    ADMIN,

    // can create organizations and deploy apps
    USER
}
