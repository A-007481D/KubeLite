server:
  port: 8080

spring:
  application:
    name: kubelite
  datasource:
    url: jdbc:postgresql://localhost:5432/kubelite
    username: postgres
    password: malik
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

